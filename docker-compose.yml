services:
  db:
    image: mysql:8.0
    container_name: resqdrive-mysql
    restart: always
    environment:
      MYSQL_DATABASE: resqdrive
      MYSQL_ROOT_PASSWORD: rootpassword
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
